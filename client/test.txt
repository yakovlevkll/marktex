#Что-то происходит
##Что-то однозначно произошло

*Кто бы мог _подумать_*

Нужно писать без остановки, чтобы строки стояли рядом, как оловянные солдатики. Иначе он разбивает их на абзацы. Абзац с новой строки. Логично же?



А вот куча новых строк ему не помеха. И это хорошо. Не так хорошо, как $E=mc^2$ но всё же.

```python
def pythonWorker():
    return cozy.stuff
```

$$D=b^2-4*a*c$$
$$x_{1,2}=\df{-b\pm\sqrt{D}}{2*a}$$

```javascript
var marktex = 'Write less, get more.'

function getSlogan() {
    return 'The only best of both worlds.'
}
```

$$\ga+\gb+\gg=180\deg$$

$\df58+\df58*\df{1}{3}*\df{1}{3}=$